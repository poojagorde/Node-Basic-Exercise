const add = (no1, no2) => {
    let sum = no1+no2;
    console.log ('Sum is :', sum);
}
const mul = (no1, no2) => {
    let mul = no1*no2;
    console.log ('Multilpication is :', mul);
}
const Calculator = () => {
    add(22, 13);
    mul(22, 13);
}
Calculator();
